{
  "name": "core-data-structures",
  "description": "Tests and implementations for common data structures.",
  "private": false,
  "version": "0.0.0",
  "lincense": "MIT",
  "repository": "git@github.com:shakalee14/core-data-structures.git",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-env": "^1.1.4",
    "babel-register": "^6.18.0",
    "chai": "~1.8.0",
    "chai-change": "^2.1.2",
    "mocha": "2.0.1"
  },
  "scripts": {
    "build": "babel src -d lib",
    "btest": "babel src -d lib && mocha --compilers js:babel-register ./spec/*.js",
    "test": "mocha --compilers js:babel-register ./spec/*.js",
    "test:python": "cd python && nosetests && cd .."
  },
  "dependencies": {
    "chai": "^1.8.1",
    "chai-as-promised": "^6.0.0",
    "sha": "^2.0.1",
    "sha256": "^0.2.0"
  }
}
